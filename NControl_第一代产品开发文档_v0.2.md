# NControl 第一代产品开发文档

**Windows 配置、优化、精简、修复与工具统一入口**

> [!IMPORTANT]
> 本文应与 `ncontrol_static_prototype.html` 一同提供给开发 AI。HTML 负责表达视觉与交互参考，本文负责约束产品定位、设计逻辑、业务对象、业务流程和长期边界。

> [!NOTE]
> **文档定位**
>
> 本文件用于统一产品方向和开发边界，重点回答“产品为什么这样组织、功能如何进入产品、用户如何完成操作、后续如何持续扩展”。不在第一代阶段深究具体代码实现、框架细节和完整检测机制。

| 项目 | 内容 |
| --- | --- |
| 产品代号 | NControl（暂定） |
| 文档版本 | v0.2 |
| 编制日期 | 2026年8月7日 |
| 视觉参考 | ncontrol_static_prototype.html |
| 适用阶段 | 第一代功能实现与后续持续迭代 |

**核心判断：功能可以大量吸收开源成果，但产品结构、业务规则、用户流程和功能入口必须掌握在自己手中。**

# 0 执行摘要

> 先固定经久不衰的产品底座，再快速吸收开源功能。

> [!NOTE]
> **第一代的正确目标**
>
> 不追求一次性做出完美检测、完整回滚和动态插件系统。第一代只要求：统一入口成立、功能真实可执行、交互流程一致、风险表达清楚、后续增加功能不需要反复改产品结构。

## 0.1 本文要固定的六件事

| 固定项 | 必须形成的共识 |
| --- | --- |
| 产品定位 | 不是“优化大师”，而是 Windows 控制中心。 |
| 信息架构 | 一级导航长期稳定，新增功能进入既有分类。 |
| 功能模型 | 每项功能都是独立条目，具备统一名称、说明、分类、风险和执行入口。 |
| 执行流程 | 选择、确认、执行、结果、记录采用同一套流程。 |
| 开源边界 | 开源项目是功能来源，不得直接决定产品页面和用户流程。 |
| 迭代原则 | 第一代先实现，后续逐步增加检测、回滚、在线更新和更强治理。 |

## 0.2 第一代暂缓事项

- 不要求每项功能都能自动检测当前状态。
- 不要求每项功能都具备完整回滚能力。
- 不建设公开插件市场或第三方插件生态。
- 不承诺覆盖所有 Windows 版本、版本分支和特殊企业策略环境。
- 不制作“电脑健康分”“性能提升百分比”等缺乏可靠依据的指标。
- 不在第一代统一重写所有开源功能的底层实现。

> [!NOTE]
> **判断标准**
>
> 凡是会影响未来页面组织、功能归类、执行流程和开源功能接入方式的决策，现在就要定；仅影响实现细节、效率和高级能力的事项，可以后补。


# 0.3 技术基线（开发强约束）

> 本节只固定长期底座，不展开具体代码实现。开发 AI 不得自行更换技术路线；确需调整时，必须先说明原因、影响范围和迁移成本，由项目负责人决定。

| 项目 | 第一代固定选择 |
| --- | --- |
| 开发语言 | C# |
| 运行平台 | .NET 10 LTS |
| 桌面 UI | WPF |
| UI 描述 | XAML |
| UI 架构 | MVVM |
| MVVM 基础 | CommunityToolkit.Mvvm |
| 应用基础设施 | Microsoft.Extensions.Hosting / Dependency Injection / Logging |
| 应用形态 | 模块化单体 |
| 功能执行 | C# 原生能力 + PowerShell + Windows 系统命令/工具适配 |
| 本地配置 | JSON |
| 本地持久化 | 第一代按需使用 SQLite，优先用于任务记录、历史和后续扩展 |
| 版本控制 | Git |
| 目标平台 | Windows 10 / Windows 11，第一代优先 x64 |
| 开发与破坏性测试 | Windows 11 虚拟机 + 快照 |

## 技术底座原则

1. **WPF 只负责产品界面，不直接承载系统优化逻辑。**
2. **页面不得直接修改注册表、服务、计划任务、Appx 或调用系统命令。**
3. **所有可执行功能统一经过执行中心。**
4. **PowerShell 是快速实现和吸收开源功能的重要执行方式，但不是整个应用的架构核心。**
5. **稳定、高频、通用的能力可以逐步沉淀为 C# 原生实现；第一代不要求一次完成。**
6. **开源项目必须经过适配后接入，不允许让某个开源项目反向决定主程序结构。**
7. **第一代不建设复杂动态插件系统；先保持代码和业务模块化，后续有真实需求再开放插件接口。**
8. **第三方 UI 库可以使用，但只能作为控件和视觉实现，不得成为业务架构依赖。**
9. **优先使用成熟、官方维护、资料充足的 .NET 生态组件，避免为了“新”引入小众底座。**
10. **开发 AI 不得擅自改成 WinForms、WinUI 3、Electron、Avalonia、MAUI 或前后端分离桌面架构。**

## 推荐解决方案边界

```text
NControl
├─ App / Shell              # 主窗口、导航、主题、应用生命周期
├─ Presentation             # 页面、ViewModel、通用 UI 组件
├─ Core                     # 功能目录、任务、风险、执行接口、共享模型
├─ Modules
│  ├─ Optimization
│  ├─ Applications
│  ├─ Cleanup
│  ├─ Repair
│  └─ Tools
├─ Infrastructure           # 日志、配置、权限、进程、PowerShell 等基础能力
└─ Integrations             # Win11Debloat、其他开源来源和外部能力适配
```

> [!IMPORTANT]
> **长期判断标准**
>
> UI 可以重做，某个开源实现可以替换，Windows 的具体优化方法也会变化；但 `Core → Modules → Execution/Infrastructure → Integrations` 的边界尽量保持稳定。新功能应进入既有底座，而不是为每个功能重新发明一套实现方式。


# 1 项目定义与产品定位

> 产品先解决“用户去哪儿做什么”，再解决“底层由谁实现”。

## 1.1 产品愿景

NControl 是一个面向 Windows 个人用户和技术用户的统一控制中心，将分散在系统设置、命令行、注册表、脚本和不同开源工具中的常用能力重新组织为一个清晰、可信、可持续扩展的桌面产品。

## 1.2 产品核心价值

| 价值 | 用户感知 |
| --- | --- |
| 集中 | 优化、精简、清理、修复和常用工具不再分散。 |
| 易懂 | 用户首先看到作用和影响，而不是注册表键值和复杂命令。 |
| 可信 | 不夸大提速效果，不隐藏高风险影响，不伪造健康评分。 |
| 可控 | 用户可以按方案、分类或单项执行，而不是只有“一键全开”。 |
| 可成长 | 后续增加新功能时，不需要新增一套孤立页面和交互。 |

## 1.3 目标用户

| 用户类型 | 典型诉求 | 产品应对 |
| --- | --- | --- |
| 普通用户 | 关闭广告、清理预装、修复常见问题。 | 提供推荐方案、清晰说明和低门槛入口。 |
| 进阶用户 | 自定义系统设置、服务、更新和组件。 | 提供分类浏览、搜索和高级模式。 |
| 运维/装机用户 | 快速执行固定配置和常用修复。 | 后续提供配置复用、任务记录和批量能力。 |

## 1.4 明确不做什么

- 不做杀毒软件、驱动全自动更新器或所谓“内存神级优化器”。
- 不提供破解、激活、绕过授权和来源不明的软件安装能力。
- 不以关闭安全防护和系统更新作为默认卖点。
- 不把所有 Windows 命令简单堆成按钮墙。
- 不为了功能数量盲目导入过时、重复或无法解释的优化项。

# 2 设计逻辑与信息架构

> 静态 HTML 是视觉基准，本文固定其背后的产品逻辑。

## 2.1 一级导航长期稳定

| 入口 | 用户问题 | 主要内容 |
| --- | --- | --- |
| 首页 | 现在最常做什么？ | 推荐入口、常用工具、最近执行。 |
| 一键优化 | 我不想逐项研究，怎么选？ | 轻度、推荐、深度方案与自定义选择。 |
| 系统设置 | 我想调整某个 Windows 行为。 | 任务栏、资源管理器、隐私、更新、性能、游戏。 |
| 应用管理 | 哪些应用和组件可以处理？ | 预装应用、已安装软件、Windows 组件。 |
| 清理维护 | 哪些无用内容可以清理？ | 临时文件、缓存、更新残留和日志。 |
| 系统修复 | 系统或网络出现问题怎么办？ | DISM、SFC、更新、商店和网络修复。 |
| 实用工具 | 日常运维工具在哪里？ | 网络、进程、服务、计划任务、可选功能等。 |

> [!NOTE]
> **导航原则**
>
> 新增功能原则上不得增加一级导航。先判断它属于设置、应用、清理、修复还是工具。只有产品形态发生重大变化时，才允许调整一级结构。

## 2.2 首页是任务入口，不是功能仓库

- 首页优先展示用户最常完成的任务，而不是所有分类。
- 首页卡片只承担跳转和发起，不承载复杂配置。
- “最近执行”用于建立连续性和信任，后续可扩展为任务记录。
- 没有可靠检测能力之前，不展示虚构的系统健康分和性能评分。

## 2.3 渐进式展示

**流程：** 推荐方案 → 功能分类 → 单项设置 → 影响说明 → 执行结果

普通用户可以停留在推荐方案；进阶用户可以进入分类和单项；高风险内容只在高级区域出现。复杂度随用户主动深入而增加。

## 2.4 统一视觉语义

| 元素 | 固定含义 |
| --- | --- |
| 紫蓝主色 | 主操作、选中状态和品牌识别。 |
| 绿色 | 成功、推荐或安全结果。 |
| 橙色 | 谨慎、条件性影响或需要注意。 |
| 红色 | 高风险、不可逆或可能降低安全性。 |
| 灰色 | 次级信息、未选择、按需功能。 |

# 3 产品底座与长期边界

> 底座不是某个开源项目，而是稳定的产品规则。

> [!NOTE]
> **核心原则**
>
> ZyperWin++、Win11Debloat及未来接入的任何项目，都只能提供功能实现。它们不能直接决定 NControl 的导航、页面命名、风险标准、执行流程和品牌体验。

## 3.1 四层产品结构（业务视角）

| 层级 | 职责 | 长期稳定性 |
| --- | --- | --- |
| 产品界面层 | 展示页面、导航、卡片、选择、确认和结果。 | 长期稳定，用户可见。 |
| 功能目录层 | 登记所有功能的名称、分类、说明、风险和执行入口。 | 长期稳定，是持续扩展核心。 |
| 执行中心层 | 统一接收任务、调用实现、收集结果和生成记录。 | 稳定演进，不被单一来源绑定。 |
| 功能实现层 | 承载开源脚本、系统命令、原生能力和自研功能。 | 可替换、可更新、可并存。 |

## 3.2 必须遵守的底座规则

1. 页面不直接拥有功能。功能先进入统一目录，再由页面展示。
1. 每项功能只解决一个清晰问题，避免一个开关同时修改多类系统行为。
1. 预设方案只是功能集合，不复制或另写一套执行逻辑。
1. 所有执行都经过统一执行中心，不允许某个页面自行弹出脚本或绕过记录。
1. 开源来源必须经过适配和重新说明，不直接暴露对方的页面结构。
1. 功能来源可以替换；替换后页面名称、用户操作和任务记录不应改变。
1. 第一代暂未实现的检测、回滚字段仍应在功能模型中预留位置。

## 3.3 模块化单体的产品含义

第一代仍然是一个完整应用，但业务上划分为相对独立的模块。模块之间共享统一功能目录、执行中心和任务记录，不互相直接控制。

| 模块 | 边界 |
| --- | --- |
| 优化模块 | 系统体验、隐私、任务栏、资源管理器、更新、性能和游戏。 |
| 应用模块 | 预装应用、已安装软件、系统组件和软件入口。 |
| 清理模块 | 可删除数据、缓存、临时文件和维护任务。 |
| 修复模块 | 针对故障恢复系统、更新、商店和网络能力。 |
| 工具模块 | 查看、诊断或操作日常 Windows 管理对象。 |
| 记录与设置 | 统一记录任务、保存偏好和管理产品行为。 |

> [!NOTE]
> **为什么这样经久不衰**
>
> 未来 UI 可以升级、功能来源可以更换、检测和回滚可以补充，但只要功能目录、模块边界和执行流程不变，产品无需重新推倒。

# 4 核心业务对象

> 开发过程中统一使用这些概念，避免每个页面自创一套规则。

## 4.1 功能项 Function

功能项是产品最小业务单元。用户看到的每一个开关、按钮或可执行操作，本质上都是一个功能项。

| 字段 | 业务含义 | 第一代要求 |
| --- | --- | --- |
| 唯一标识 | 长期识别该功能，不随显示名称变化。 | 必须 |
| 名称 | 用户能理解的动作名称。 | 必须 |
| 分类 | 决定它出现在哪个模块和分组。 | 必须 |
| 说明 | 解释作用、适用场景和可能影响。 | 必须 |
| 风险等级 | 决定颜色、提示、确认方式和是否进入预设。 | 必须 |
| 实现来源 | 自研、系统命令或某个开源来源。 | 必须 |
| 管理员权限 | 执行是否需要提升权限。 | 必须 |
| 重启要求 | 无需重启、重启资源管理器或重启系统。 | 必须 |
| 当前状态 | 是否已应用或当前值。 | 预留，后补 |
| 恢复方式 | 撤销或恢复原状。 | 预留，后补 |

## 4.2 方案 Preset

方案是若干功能项的组合，主要服务不愿逐项选择的用户。方案不产生新的功能实现，只引用现有功能。

| 方案 | 包含内容 | 限制 |
| --- | --- | --- |
| 轻度优化 | 广告、推荐、界面和低风险个性化设置。 | 不得包含服务、安全、更新关闭等高影响项。 |
| 推荐优化 | 适合多数个人电脑的隐私、体验和常见后台调整。 | 只允许安全与推荐等级，少量谨慎项需单独说明。 |
| 深度优化 | 服务、更新、性能、电源及更强系统调整。 | 必须逐组展示，不可默认全选高风险项。 |
| 自定义方案 | 用户选择并保存的功能集合。 | 后续版本实现保存与导入导出。 |

## 4.3 任务 Task

每次点击执行，都生成一个任务。任务可以包含一个功能或多个功能，是进度、结果和历史记录的统一载体。

**流程：** 创建任务 → 确认清单 → 逐项执行 → 汇总结果 → 写入记录

## 4.4 模块 Module 与来源 Source

| 对象 | 作用 |
| --- | --- |
| 模块 | 决定功能在产品中的业务归属，例如优化、应用、清理和修复。 |
| 来源 | 记录功能从哪里来、使用哪个版本、遵循什么许可证、何时验证。 |

# 5 统一业务流程

> 不同功能可以有不同实现，但用户操作必须保持一致。

## 5.1 标准执行流程

**流程：** 发现功能 → 选择功能 → 查看影响 → 确认执行 → 查看进度 → 得到结果

| 阶段 | 产品行为 |
| --- | --- |
| 发现 | 通过首页、搜索、方案或分类找到功能。 |
| 选择 | 单项切换、批量勾选或选择预设方案。 |
| 影响说明 | 展示风险、管理员权限、重启要求和主要影响。 |
| 确认 | 列出实际将执行的功能，不使用模糊的“一键优化”确认。 |
| 执行 | 统一进度面板逐项展示成功、失败、跳过和当前任务。 |
| 结果 | 汇总成功数、失败数、需重启项及日志入口。 |
| 记录 | 生成任务记录，便于后续排查和重复操作。 |

## 5.2 单项设置流程

用户切换某个设置时，界面先进入“待应用”状态，而不是立即在后台悄悄修改。页面底部统一出现已选择数量和应用按钮。这样用户知道本次会改变什么，也便于未来加入检测、冲突提示和回滚。

## 5.3 即时工具流程

Ping、DNS 查询、查看端口等工具不适用“批量应用”流程，可以点击即运行。但仍应统一展示输入条件、执行状态、结果和错误，不直接弹出黑色命令窗口作为最终体验。

## 5.4 错误和中断

| 情况 | 处理规则 |
| --- | --- |
| 单项失败 | 记录失败原因，默认继续执行后续项目。 |
| 关键前置失败 | 停止依赖该前置条件的项目，其他无关项目可继续。 |
| 用户停止 | 停止尚未开始的项目；已经完成的项目不假装撤销。 |
| 需要重启 | 任务完成后统一汇总，不在每项执行中反复弹窗。 |
| 来源不可用 | 明确显示该功能暂不可执行，不允许无响应或静默失败。 |

# 6 页面与业务逻辑

> 视觉参考来自静态 HTML，页面功能按以下规则落地。

## 6.1 首页

- 顶部主区展示产品价值和两个最高频动作。
- 四个核心任务卡：推荐优化、系统精简、清理空间、系统修复。
- 常用工具区最多展示四至六个高频入口，支持后续个性化。
- 最近执行展示任务结果，不展示无法验证的系统评分。

## 6.2 一键优化

- 方案卡片先解释适用人群和影响，再允许选择。
- 选择方案后可以查看具体功能清单，并允许取消个别项目。
- 深度优化不得作为默认选项，也不得暗示“越深越快”。
- 底部执行栏持续显示已选择数量和开始执行入口。

## 6.3 系统设置

- 使用分类标签和搜索承载大量设置，不扩张一级导航。
- 每项设置使用统一卡片：名称、说明、风险标签、操作控件。
- 高风险项目显示完整影响，并进入高级区域。
- 第一代状态未知时，不用开关伪装成当前系统真实状态；可采用“选择应用”表达。

## 6.4 应用管理

- 区分预装应用、普通软件和 Windows 组件，避免混在同一列表。
- 应用标记为推荐删除、按需保留、建议保留或必须保留。
- 默认不选择 Microsoft Store、关键运行库和系统依赖。
- 卸载前显示实际选中列表，不以预计释放空间作为唯一判断。

## 6.5 清理维护

- 清理项按来源分组：系统临时文件、应用缓存、更新残留、日志和回收站。
- 明确区分“可以安全清理”和“清理后可能影响排错/恢复”的内容。
- 第一代可以先提供固定清理动作；后续再增加扫描、大小统计和排除项。

## 6.6 系统修复

- 按问题组织，而不是只展示 DISM、SFC 等命令名称。
- 修复卡片说明适用症状、预计影响和是否需要重启。
- 网络修复分为清缓存、重置协议、重新获取地址等独立动作。
- 多步骤修复形成一个任务，统一展示执行顺序。

## 6.7 实用工具

- 工具优先覆盖高频、确定和可解释的运维需求。
- 查看类工具与修改类工具在视觉和确认方式上区分。
- 避免把所有系统命令都做成按钮；只有能形成稳定体验的工具才进入产品。

# 7 风险、权限与信任规则

> 第一代即使没有完整检测，也必须建立可信边界。

| 等级 | 定义 | 展示与执行规则 |
| --- | --- | --- |
| 安全 | 一般仅影响界面和非关键偏好。 | 可进入轻度和推荐方案，普通确认。 |
| 推荐 | 有明确收益，影响范围可理解。 | 可进入推荐方案，说明主要变化。 |
| 谨慎 | 可能影响部分功能、性能、耗电或维护。 | 默认不选，需显示条件和影响。 |
| 高风险 | 可能降低安全性、更新能力或稳定性。 | 仅高级区域，单独确认，不进入预设。 |
| 实验性 | 效果依赖系统版本或验证不足。 | 默认隐藏或明确测试标识。 |

## 7.1 管理员权限

需要管理员权限不等于高风险。产品应分别表达“权限要求”和“风险等级”。普通用户在执行阶段统一授权，避免每项功能反复弹出权限提示。

## 7.2 高风险功能治理

- 关闭 Defender、UAC、安全缓解和系统更新等功能不得进入任何默认推荐。
- 不得用“性能优化”掩盖安全性下降。
- 必须说明具体影响，不能只写“可能存在风险”。
- 对于来源不明确、验证不足或已经过时的优化项，宁可暂不导入。

## 7.3 文案规则

| 不推荐写法 | 推荐写法 |
| --- | --- |
| 极致提速、释放全部性能 | 关闭窗口动画，使界面操作更直接。 |
| 深度清理系统垃圾 | 清理 Windows 临时文件和更新缓存。 |
| 优化 CPU 性能 | 切换到高性能电源计划，可能增加耗电和发热。 |
| 关闭无用服务 | 停止 SysMain；部分设备上的应用预加载可能变慢。 |

# 8 开源功能吸收与替换规则

> “快速跟上功能”依靠开放来源，“长期可持续”依靠统一治理。

## 8.1 接入原则

1. 先判断功能是否值得进入产品，再判断从哪个项目吸收。
1. 相同功能只保留一个产品入口，底层可以存在多个候选实现。
1. 导入后重新编写用户名称、说明、风险和适用场景。
1. 保留来源、版本、许可证和验证日期，便于后续更新。
1. 上游停止维护时，只替换功能实现，不改变用户入口。
1. 不得把开源项目完整页面嵌入产品，形成割裂的“软件套软件”。

## 8.2 功能接入流程

**流程：** 发现来源 → 筛选价值 → 核对授权 → 归一化说明 → 接入执行 → 验证发布

## 8.3 来源台账

| 记录项 | 用途 |
| --- | --- |
| 来源项目及链接 | 追溯代码和功能原始出处。 |
| 许可证 | 判断能否修改、集成和发布。 |
| 接入版本/提交 | 避免上游更新后无法确认差异。 |
| 对应功能项 | 明确该来源支撑哪些产品功能。 |
| 本地修改 | 记录为了产品适配做过哪些改变。 |
| 验证系统版本 | 记录在哪些 Windows 环境完成测试。 |

> [!NOTE]
> **对 ZyperWin++ 与 Win11Debloat 的定位**
>
> 它们都是重要功能池和研究对象。第一代可以快速复用成熟实现，但不继承其信息架构，也不把其项目名称暴露为主产品入口。

# 9 第一代范围与优先级

> 以“功能形成完整闭环”为完成标准，不以功能总数为唯一指标。

## 9.1 第一代必须具备

| 范围 | 最低要求 |
| --- | --- |
| 产品外壳 | 完成静态 HTML 对应的主要页面、导航、搜索和执行面板。 |
| 功能目录 | 所有功能通过统一资料进入页面，不允许随意硬编码页面专属逻辑。 |
| 执行中心 | 能够执行单项与批量任务，显示成功、失败和输出。 |
| 优化功能 | 轻度、推荐、自定义方案可执行，覆盖主要常用分类。 |
| 应用管理 | 能够处理主要预装应用，明确保留项。 |
| 清理维护 | 提供常用、安全、可解释的清理动作。 |
| 系统修复 | 提供系统文件、映像、更新、商店和网络修复入口。 |
| 实用工具 | 提供一批稳定、常用的查看与诊断工具。 |
| 任务记录 | 保存每次任务的时间、项目和结果。 |

## 9.2 功能导入优先级

| 优先级 | 内容 | 理由 |
| --- | --- | --- |
| P0 | 广告推荐、任务栏、资源管理器、隐私、常见 Appx、临时文件、DISM/SFC、网络修复。 | 高频、易理解、价值明确。 |
| P1 | Windows 更新、电源、游戏、Edge、服务、可选功能、启动项和计划任务。 | 价值较高，但影响范围更大。 |
| P2 | 安全缓解、Defender 深度控制、删除系统组件、实验性性能参数。 | 高风险或需要更多验证。 |

## 9.3 第一代完成定义

- 页面中不存在无法使用的主要按钮和占位入口。
- 所有已发布功能可以正常发起、反馈结果并写入任务记录。
- 功能名称、说明、分类和风险表达保持一致。
- 高风险功能不会被默认选择或混入推荐方案。
- 开源来源和许可证台账完整。
- 第一代不因缺少状态检测而假装显示真实开关状态。

# 10 持续迭代路线

> 后续能力围绕现有底座自然生长，不重新改变产品结构。

| 阶段 | 主要目标 | 对现有产品的影响 |
| --- | --- | --- |
| 第一代：功能成立 | 统一 UI、功能目录、执行中心、核心功能和任务记录。 | 建立产品骨架。 |
| 第二阶段：状态感知 | 增加当前状态检测、扫描结果、空间统计和兼容性提示。 | 在现有卡片补状态，不改页面结构。 |
| 第三阶段：恢复与配置 | 增加单项恢复、批次回滚、配置保存、导入导出。 | 扩展任务和功能字段。 |
| 第四阶段：更新治理 | 功能模块更新、上游差异管理、版本兼容和灰度发布。 | 替换实现层，不改变用户入口。 |
| 第五阶段：专业能力 | 命令行、批量配置、远程或企业场景。 | 复用同一功能目录和执行中心。 |

> [!NOTE]
> **迭代检查问题**
>
> 每次准备增加新能力时，都要问：它是在丰富现有模块，还是在破坏既有产品结构？如果必须新增一级入口或独立执行流程，应先重新评估。

## 10.1 后续优先补强项

1. 状态检测：让设置项反映当前系统真实状态。
1. 回滚与备份：把“可执行”升级为“可恢复”。
1. 兼容性：按 Windows 版本、版本号和系统策略判断可用性。
1. 冲突关系：避免互相抵消或重复的功能同时执行。
1. 配置方案：保存用户选择，并支持重复部署。
1. 功能更新：独立跟进开源上游和 Windows 版本变化。

# 11 AI 开发协作规则

> 让 AI 负责实现，但由文档控制边界和一致性。

## 11.1 每个开发任务必须说明

| 项目 | 说明 |
| --- | --- |
| 目标 | 本次新增或修改的用户能力是什么。 |
| 所属模块 | 优化、应用、清理、修复、工具或基础能力。 |
| 涉及页面 | 哪些现有页面需要变化。 |
| 业务流程 | 用户从哪里进入、如何确认、如何得到结果。 |
| 风险等级 | 是否默认推荐、是否需要高级确认。 |
| 允许修改范围 | 限定 AI 只修改必要区域，避免无关重构。 |
| 验收场景 | 成功、失败、取消、无权限和需要重启时如何表现。 |

## 11.2 AI 不得自行改变的内容

- 不得自行增加一级导航或改变模块名称。
- 不得跳过统一执行流程，直接在页面中实现孤立操作。
- 不得将高风险功能加入推荐方案。
- 不得为追求“完整”一次性重构无关模块。
- 不得删除来源、许可证和功能说明信息。
- 不得使用虚假性能数据或健康评分填充页面。

## 11.3 推荐开发节奏

**流程：** 确定需求 → 补业务说明 → AI 实现 → 本机测试 → 修正问题 → 更新台账

开发工作以“小步可运行”为原则。一次集中完成一个页面、一个模块或一组高度相关的功能，避免 AI 在大型跨模块任务中产生不可控改动。

# 12 验收标准

> 第一代以可用、统一、可信为核心。

## 12.1 产品一致性

- 所有页面使用同一导航、间距、卡片、风险标签和按钮规则。
- 相同类型的功能具有相同的选择、确认、进度和结果体验。
- 用户不需要知道功能来自哪个脚本或开源项目。

## 12.2 功能可用性

- 每个已发布功能能被正常触发，并能明确返回成功或失败。
- 管理员权限、路径错误和来源缺失均有可理解提示。
- 批量执行中单项失败不会导致界面卡死或结果丢失。

## 12.3 风险与信任

- 推荐方案不包含高风险项目。
- 修改安全、更新和系统关键组件时有明确说明。
- 不可逆或暂不支持恢复的功能如实标注。

## 12.4 可持续性

- 新增功能不需要新建一套独立页面结构。
- 替换开源来源不影响用户入口和功能名称。
- 功能台账、来源台账和任务记录能够追溯。

> [!NOTE]
> **第一代发布口径**
>
> 第一代应定位为“功能型测试版本”，强调统一入口和常用能力，不宣称覆盖所有电脑、所有系统版本或保证所有优化都能提升性能。

# 附录 A 功能接入检查表

> 任何新功能进入产品前，至少回答以下问题。

| 序号 | 检查问题 |
| --- | --- |
| 1 | 它解决的具体用户问题是什么？ |
| 2 | 属于优化、应用、清理、修复还是工具？ |
| 3 | 是否已经存在相同或高度重叠的功能？ |
| 4 | 用户能否从名称直接理解它做什么？ |
| 5 | 主要影响是什么，适用于哪些场景？ |
| 6 | 风险等级是什么，能否进入推荐方案？ |
| 7 | 是否需要管理员权限或系统重启？ |
| 8 | 执行成功和失败如何反馈？ |
| 9 | 功能实现来自哪里，许可证是否允许使用？ |
| 10 | 上游更新或停止维护后能否替换实现？ |
| 11 | 第一代是否需要检测或恢复；如不需要，是否已预留？ |
| 12 | 在哪些 Windows 环境完成过验证？ |

## 示例：关闭开始菜单推荐

| 项目 | 示例内容 |
| --- | --- |
| 功能名称 | 关闭开始菜单推荐 |
| 所属模块 | 系统设置 → 任务栏与开始菜单 |
| 用户说明 | 减少开始菜单中的推荐应用、文件和推广内容。 |
| 风险等级 | 推荐 |
| 默认方案 | 轻度优化、推荐优化 |
| 权限 | 需要管理员权限 |
| 重启 | 可能需要重启资源管理器 |
| 实现来源 | 系统设置/注册表或开源适配实现 |
| 第一代结果 | 执行成功、执行失败、需要重新登录。 |
| 后续预留 | 当前状态检测和恢复默认值。 |

# 附录 B 视觉原型使用说明

> 静态 HTML 是开发参考，不是一次性页面截图。

参考文件：ncontrol_static_prototype.html

| 原型中已确定 | 开发时允许调整 |
| --- | --- |
| 左侧导航、顶部搜索、内容区和底部执行栏的总体结构。 | 在不改变信息架构的前提下优化间距、响应和细节。 |
| 首页、优化、设置、应用、清理、修复和工具页面的基本形态。 | 根据真实功能数量调整卡片、列表和分页方式。 |
| 紫蓝主色、低饱和背景、风险颜色和可信专业风格。 | 品牌名称、图标、具体色值和动效细节。 |
| 统一执行弹窗与任务进度表达。 | 后续可演进为侧边任务中心或后台任务面板。 |

> [!NOTE]
> **原型使用边界**
>
> 开发时不得把每个原型卡片写成独立、不可复用的页面代码。原型展示的是组件和业务模式，真实产品应通过统一功能目录持续生成和复用。

## 最终产品原则

**功能来源不断变化，产品体验保持稳定；底层能力不断增强，用户学习成本不增加；第一代先把功能做出来，后续能力都能在既有底座上自然补齐。**

— 文档结束 —
