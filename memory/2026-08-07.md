# 2026-08-07

## NControl 第一代最小闭环(自主开发任务)

用户下达了 NControl 自主开发规则:读文档 → 拆任务写 PROJECT_TASKS.md → 逐项完成不中途停 → 编译验证 → 全部完成才收尾。

**输入**:桌面上的《NControl_第一代产品开发文档_v0.2.md》+ `ncontrol_static_prototype.txt`(HTML 原型),已归档到工作区。

**产出**(提交 43d4e78,工作树干净):
- 解决方案 9 项目:`NControl.slnx`,src 下 Core / Infrastructure / Modules(Optimization、Applications、Cleanup、Repair、Tools)×5 / Presentation / App,外加 tools/NControl.SmokeTest。
- 技术基线:net10.0-windows WPF + MVVM(CommunityToolkit.Mvvm 8.4.0)+ Microsoft.Extensions.Hosting/DI/Logging + Microsoft.Data.Sqlite 10(SQLitePCLRaw 升 2.1.12 修复 NU1903 高危漏洞)。
- 功能目录 70 项、预设 4 个(轻度 8 / 推荐 18 / 深度 / 应用清理),高风险(advanced.*)不进任何预设。
- 执行中心:PowerShell 与 cmd 双执行提供程序、UAC 提权(输出走临时文件)、逐项进度/日志/取消、SQLite 任务记录(%LocalAppData%\NControl\ncontrol.db)。
- 页面:首页、一键优化、系统设置(分类芯片+高级区)、应用管理(真实 Get-AppxPackage 扫描)、清理维护、系统修复、实用工具(即时工具)、任务记录、设置、关于(来源台账)。
- 冒烟测试 28 项全 PASS(目录完整性/预设引用/无高风险/执行成功+失败路径/SQLite 持久化/搜索)。
- 启动验证:窗口真实渲染(句柄非零 + 像素扫描主色/背景)。

**关键坑**:
1. MainViewModel 构造期间经 `IServiceProvider.GetService` 解析 HomeViewModel,而 HomeViewModel 又依赖 MainViewModel → 单例构造期循环依赖,启动死锁(进程存活但窗口不可见)。修复:引入 `NavigationService`(Main 构造完成后回填),页面 VM 全部改依赖它。
2. PowerShell 5.1 的 Add-Type 不支持 C# 字符串插值。
3. WPF 无 CharacterSpacing 属性;XAML 中 Text 属性与元素体不能同时设置。
4. .NET 10 SDK 默认生成 .slnx 而非 .sln。

**遗留**:BOOTSTRAP 身份对话(名字/性格/emoji)仍未完成,挂起中;待用户有空时补齐。应用数据目录 %LocalAppData%\NControl,配置 src/NControl.App/appsettings.json。
# 2026-08-07

## NControl 第一代最小闭环(自主开发任务)

用户下达了 NControl 自主开发规则:读文档 → 拆任务写 PROJECT_TASKS.md → 逐项完成不中途停 → 编译验证 → 全部完成才收尾。

**输入**:桌面上的《NControl_第一代产品开发文档_v0.2.md》+ `ncontrol_static_prototype.txt`(HTML 原型),已归档到工作区。

**产出**(提交 43d4e78,工作树干净):
- 解决方案 9 项目:`NControl.slnx`,src 下 Core / Infrastructure / Modules(Optimization、Applications、Cleanup、Repair、Tools)×5 / Presentation / App,外加 tools/NControl.SmokeTest。
- 技术基线:net10.0-windows WPF + MVVM(CommunityToolkit.Mvvm 8.4.0)+ Microsoft.Extensions.Hosting/DI/Logging + Microsoft.Data.Sqlite 10(SQLitePCLRaw 升 2.1.12 修复 NU1903 高危漏洞)。
- 功能目录 70 项、预设 4 个(轻度 8 / 推荐 18 / 深度 / 应用清理),高风险(advanced.*)不进任何预设。
- 执行中心:PowerShell 与 cmd 双执行提供程序、UAC 提权(输出走临时文件)、逐项进度/日志/取消、SQLite 任务记录(%LocalAppData%\NControl\ncontrol.db)。
- 页面:首页、一键优化、系统设置(分类芯片+高级区)、应用管理(真实 Get-AppxPackage 扫描)、清理维护、系统修复、实用工具(即时工具)、任务记录、设置、关于(来源台账)。
- 冒烟测试 28 项全 PASS(目录完整性/预设引用/无高风险/执行成功+失败路径/SQLite 持久化/搜索)。
- 启动验证:窗口真实渲染(句柄非零 + 像素扫描主色/背景)。

**关键坑**:
1. MainViewModel 构造期间经 `IServiceProvider.GetService` 解析 HomeViewModel,而 HomeViewModel 又依赖 MainViewModel → 单例构造期循环依赖,启动死锁(进程存活但窗口不可见)。修复:引入 `NavigationService`(Main 构造完成后回填),页面 VM 全部改依赖它。
2. PowerShell 5.1 的 Add-Type 不支持 C# 字符串插值。
3. WPF 无 CharacterSpacing 属性;XAML 中 Text 属性与元素体不能同时设置。
4. .NET 10 SDK 默认生成 .slnx 而非 .sln。

**遗留**:BOOTSTRAP 身份对话(名字/性格/emoji)仍未完成,挂起中;待用户有空时补齐。应用数据目录 %LocalAppData%\NControl,配置 src/NControl.App/appsettings.json。

---

## 第二轮:参考 ZyperWin++ 完善功能(19:54-20:03)

用户提供参考:`src\ZyperWin-next`(ZyperWin++4.2 源码 + Release 脚本包 + 三个 txt)+ `ZyperWin++当前功能统计表.xlsx`(工作区根)。

**参考解析**:
- xlsx 无法直接读 → 用 outputs 目录里的 `.inspect.ndjson` 提取:153 项功能 / 7 分类(外观 29、性能 46、安全 10、Edge 12、系统 15、更新 7、隐私 34)。
- `ZyperData.xml`(104KB)用 PowerShell 脚本解析出全部 154 项注册表操作(RegWrite/RegDelete/服务),CSV 临时输出。
- 三个 txt:时钟显示秒(纳入)、经典右键(已重复)、32G 关内存压缩(纳入);私人远程服务器信息(luomup.fun)排除不纳入产品。
- `DisableWD.bat`(关闭 Defender,10+ 服务 Start=4):经评估不引入——违背文档 §1.4/§7.2,台账注明。

**导入方式**(严格按文档 §8:去重、重写名称/说明/风险、只参考注册表操作、不继承对方 UI/流程):
- Optimization 模块新增 4 个注册文件:OptimizationFeaturesExplorer.cs(任务栏/资源管理器)、Performance.cs(性能/系统/更新/Edge)、Privacy.cs(隐私 22 项)、Security.cs(高级高风险 8 项)。
- 功能目录 70 → **170 项**;新增分类:Edge 浏览器、系统设置(动态出现于系统设置页芯片)。
- 预设扩充(仍不含任何高风险):轻度 8→11、推荐 18→35、深度 37→65。
- 来源台账新增 ZyperWin++ 条目:许可证未公开待核、版本 4.2(2026-08-02 数据)、本地修改说明、待真机验证。
- 高风险治理:UAC 从不通知/关 SmartScreen/关安全警告/关防火墙/关内存完整性/关 VBS/关系统还原/从不检查更新 → 全部仅"高级"分类、单独确认、不进预设。

**验证**:冒烟测试断言更新(目录 ≥150、高级区不允许 Safe/Recommended 级、预设无高风险)全 PASS;全量构建 0 警告 0 错误;启动渲染验证通过(主色 60 点/背景 6111 点)。截图 ncontrol_v2_screenshot.png。已提交 Git。

**经验**:解析 xlsx 首选 outputs 里的 .inspect.ndjson(结构化的 workbook/table 提取),避免装第三方库;PowerShell 脚本解析 XML + Set-Content CSV 可行;Add-Type C# 编译对 PowerShell 5.1 有语法限制(无字符串插值)。
