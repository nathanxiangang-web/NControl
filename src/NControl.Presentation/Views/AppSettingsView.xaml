<UserControl x:Class="NControl.Presentation.Views.AppSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock FontSize="20" FontWeight="SemiBold" Text="设置" />
        <TextBlock Text="应用数据与基础信息。" Style="{StaticResource MutedText}" Margin="0,4,0,14" />

        <Border Style="{StaticResource Card}" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="数据目录" FontSize="14" FontWeight="SemiBold" />
                <TextBlock Text="任务记录以 SQLite 保存在以下位置:" Style="{StaticResource MutedText}" Margin="0,6,0,0" />
                <Border Background="{StaticResource PanelSoftBrush}" CornerRadius="10" Padding="12,10" Margin="0,10,0,0">
                    <TextBlock Text="{Binding DataFolder}" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" />
                </Border>
                <Button Content="打开数据目录" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Left"
                        Margin="0,12,0,0" Command="{Binding OpenDataFolderCommand}" />
            </StackPanel>
        </Border>

        <Border Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Text="任务记录数据库" FontSize="14" FontWeight="SemiBold" />
                <Border Background="{StaticResource PanelSoftBrush}" CornerRadius="10" Padding="12,10" Margin="0,10,0,0">
                    <TextBlock Text="{Binding DatabasePath}" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" />
                </Border>
                <TextBlock Text="第一代按需使用 SQLite,优先用于任务记录、历史和后续扩展。" Style="{StaticResource MutedText}" Margin="0,10,0,0" />
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
