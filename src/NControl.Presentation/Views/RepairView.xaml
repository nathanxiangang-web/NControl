<UserControl x:Class="NControl.Presentation.Views.RepairView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock FontSize="20" FontWeight="SemiBold" Text="系统修复" />
        <TextBlock Text="集中运行 Windows 自带工具,处理常见系统和网络问题。" Style="{StaticResource MutedText}" Margin="0,4,0,14" />

        <!-- 修复卡片 -->
        <ItemsControl ItemsSource="{Binding Cards}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="2" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource Card}" MinHeight="180" Margin="0,0,14,14">
                        <StackPanel>
                            <Border Style="{StaticResource IconBox}" Margin="0,0,0,14">
                                <TextBlock Text="{Binding Glyph}" FontSize="17" FontWeight="Bold"
                                           Foreground="{StaticResource PrimaryBrush}"
                                           HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <TextBlock Text="{Binding Title}" FontSize="15" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding Description}" Style="{StaticResource MutedText}" Margin="0,8,0,14" />
                            <TextBlock Text="{Binding MetaText}" Style="{StaticResource SubtleText}" Margin="0,0,0,10" />
                            <Button Content="开始修复" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Left"
                                    Command="{Binding RunCommand}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- 网络修复 -->
        <TextBlock Text="网络修复" Style="{StaticResource SectionTitle}" />
        <TextBlock Text="执行前会展示影响说明;执行结果写入任务记录。" Foreground="{StaticResource MutedBrush}"
                   FontSize="12" Margin="0,-40,0,12" HorizontalAlignment="Right" />
        <ItemsControl ItemsSource="{Binding NetworkActions}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="4" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{StaticResource PanelSoftBrush}" CornerRadius="12" Padding="14,16" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="{Binding Title}" FontSize="12.5" FontWeight="SemiBold" TextWrapping="Wrap" />
                            <Button Content="执行" Style="{StaticResource GhostButton}" HorizontalAlignment="Left"
                                    Padding="0" Margin="0,10,0,0" Command="{Binding RunCommand}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
